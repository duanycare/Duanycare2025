# 🧬 YCARE Backend ENV Example

# PostgreSQL Database
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE

# JWT Secret (cho xác thực)
JWT_SECRET=your_jwt_secret_here

# Plugin toggle (nếu muốn hỗ trợ toggle qua env)
ENABLE_PLUGIN_VOUCHER=true
ENABLE_PLUGIN_KPI=true
ENABLE_PLUGIN_LOYALTY=true

# Cấu hình domain cho frontend (nếu dùng cho CORS, email, redirect...)
FRONTEND_URL=https://ycare-frontend.vercel.app
